<div class="container">
  <div class="row">
    <div class="col-12 text-center">
      <h5 class="title-border pb-4">予約画面</h5>
      <p>3ヶ月先まで予約することができます。</p>
    </div>
    <div class="col-12 mt-3">
      <%= week_calendar events: @reserves do |date, reserves| %>
        <%= date.day %>
      <% end %>
    </div>
  </div>
</div>